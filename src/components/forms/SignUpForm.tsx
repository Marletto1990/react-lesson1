import { FC } from 'react';
//import { useForm } from 'react-hook-form';

export const SignUpForm: FC = () => {
	//const {control, handleSubmit, formState: {}} = useForm<>({defaultValues: }

	//)
	return <div>Форма разметки</div>;
};
